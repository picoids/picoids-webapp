/** JSX subtree for `next/og` ImageResponse (inline styles only). */
export function OgBrandMarkup() {
  return (
    <div
      style={{
        height: "100%",
        width: "100%",
        display: "flex",
        flexDirection: "column",
        alignItems: "flex-start",
        justifyContent: "center",
        background:
          "linear-gradient(135deg, #0f172a 0%, #1e293b 45%, #0f172a 100%)",
        padding: 72,
      }}
    >
      <div
        style={{
          fontSize: 72,
          fontWeight: 700,
          color: "#f8fafc",
          letterSpacing: "-0.04em",
          lineHeight: 1.1,
        }}
      >
        Picoids
      </div>
      <div
        style={{
          marginTop: 16,
          fontSize: 28,
          fontWeight: 500,
          color: "#94a3b8",
          maxWidth: 900,
        }}
      >
        Technology & consulting for cloud, applications, and AI
      </div>
    </div>
  );
}
